@model List<Product>
@{
    ViewData["Title"] = "Products";
    Layout = "~/Views/Member/_MemberLayout.cshtml";
}


<h3 class="tittle">Ürünler</h3>
<div class="inner-sec">
        <div class="left-blog-info-w3layouts-agileits text-left">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-lg-4 card my-2">
                    <a href="single.html">
                        <img src="~/Tema/resim/@item.ImageUrl" class="card-img-top img-fluid" alt="">
                    </a>
                    <div class="card-body">

                        <ul class="blog-icons ">

                            <li class="mx-0">
                                <h5 class="card-title">
                                    <a href="~/Blog/BlogReadAll/@item.ProductName">@item.ProductName</a>
                                </h5>
                                <a href="#">
                                    <i class="tittle">@Html.DisplayFor(modelitem=>item.Category.CategoryName)</i>
                                </a>
                            </li>
                             <form asp-action="ProductDetail" method="post" style="display: inline">
                            <input type="hidden" name="productId" value="@item.Id">
                            <button type="submit" class="btn btn-info btn-sm my-2 mx-2">Ürün Detayı</button>
                        </form>
                        </ul>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

